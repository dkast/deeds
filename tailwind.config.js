const colors = require("tailwindcss/colors");

module.exports = {
  theme: {
    extend: {
      colors: {
        orange: colors.orange,
      },
    },
    darkMode: "media",
  },
  variants: {},
  plugins: [],
};
